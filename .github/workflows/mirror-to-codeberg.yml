name: Mirror to Codeberg

on:
  push:
    branches: [main]

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Codeberg
        run: |
          git remote add codeberg https://flandre_scarlet:${{ secrets.CODEBERG_TOKEN }}@codeberg.org/flandre_scarlet/bsetm-dl.git
          git push -f codeberg main
